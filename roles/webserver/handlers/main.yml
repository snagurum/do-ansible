---
# handlers file for roles/webserver


     - name: "restart nginx"
       ansible.builtin.service:
          name: nginx
          state: restarted

     - name: "reload nginx"
       ansible.builtin.service:
          name: nginx
          state: reloaded
